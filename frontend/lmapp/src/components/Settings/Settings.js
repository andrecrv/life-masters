import './Settings.css';

const Settings = () => {
    return (
        <div className="settings-container">
            <h2>Settings</h2>
            <p>This is the Settings section.</p>
        </div>
    );
};

export default Settings;